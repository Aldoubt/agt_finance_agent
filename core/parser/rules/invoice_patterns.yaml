invoice_number:
  patterns:
    - "发票号码[:：]?\\s*([0-9A-Z]+)"
    - "票号[:：]?\\s*([0-9A-Z]+)"
amount:
  patterns:
    - "价税合计[:：]?\\s*[¥￥]?([0-9,.]+)"
    - "金额[:：]?\\s*[¥￥]?([0-9,.]+)"
date:
  patterns:
    - "(20\\d{2}[年/-]\\d{1,2}[月/-]\\d{1,2}日?)"
